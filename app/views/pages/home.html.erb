<% if user_signed_in? %>
<div class="container">
  <nav class="my-3">
    <div class="nav nav-tabs border-0 rounded radius" id="nav-tab" role="tablist">
      <button class="nav-link custom-tab w-50 active" id="nav-home-tab" data-bs-toggle="tab" data-bs-target="#nav-home" type="button" role="tab" aria-controls="nav-home" aria-selected="true">📦 Барај товар</button>
      <button class="nav-link custom-tab w-50" id="nav-profile-tab" data-bs-toggle="tab" data-bs-target="#nav-profile" type="button" role="tab" aria-controls="nav-profile" aria-selected="false">🚚 Барај камион</button>
    </div>
  </nav>
  <div class="tab-content" id="nav-tabContent">
    <div class="tab-pane fade show active" id="nav-home" role="tabpanel" aria-labelledby="nav-home-tab">
    <%# Ponuda na tovar %>
    <% @load_posts.each do |post| %>
      <%= link_to do %>
      <div class="d-flex align-items-center card-index mb-1 py-3 px-4 rounded bg-white">
        <p class="m-0" style="width: 7.5%"><%= post.load_date.strftime("%d/%m") %></p>
        <p class="m-0" style="width: 20%"><%= post.load_country %>, <%= post.load_city %></p>
        <p class="m-0" style="width: 7.5%"><%= post.unload_date.strftime("%d/%m") %></p>
        <p class="m-0" style="width: 20%"><%= post.load_country %>, <%= post.load_city %></p>
        <p class="m-0 text-end" style="width: 25%"><%= post.truck_type %></p>
        <p class="m-0 text-end" style="width: 10%"><%= post.length %>m</p>
        <p class="m-0 text-end" style="width: 10%"><%= post.weight %>t</p>
      </div>
      <% end %>
    <% end %>
    </div>
    <div class="tab-pane fade" id="nav-profile" role="tabpanel" aria-labelledby="nav-profile-tab">
    <%# Ponuda na kamion %>
    </div>
  </div>
</div>
<% else %>
  <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.unsplash.com/photo-1519003722824-194d4455a60c?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1750&q=80);">
    <div class="container value-prop">
      <div class="col-sm-12 col-lg-6 bg-white p-4 rounded">
        <div class="d-flex align align-items-center mb-3">
          <%= image_tag "logo.jpg", style: "height: 30px; width: 30px;"%>
          <h4 class="my-0 mx-2">Cargster</h4>
        </div>
        <h2>Едноставна размена на товар.</h2>
        <%# <p class="text-secondary">Каргстер е веб портал кој им помага на учесниците во транспортната индустрија како алатка за <strong>ефикасна комунукација</strong>.</p> %>
      <div class="d-flex">
        <%= link_to "Регистрација", new_user_registration_path, class: "btn btn-primary text-white mt-2" %>
        <%= link_to "Најава", new_user_session_path, class: "btn btn-outline-primary text-primary mt-2 mx-2" %>
      </div>
    </div>
  </div>
<% end %>
